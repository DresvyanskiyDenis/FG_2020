import gc
import wave

import librosa
import moviepy.editor as mp
import os

def extend_sample_rate_of_labels_by_interpolation(labels, needed_sample_rate):
    pass

def downgrade_sample_rate_of_labels(labels, needed_sample_rate):
    pass
